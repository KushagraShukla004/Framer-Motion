const BasicsOfMotion = () => {
  return (
    <div
      //this style is just to make everything in center
      style={{
        display: "grid",
        placeContent: "center",
        height: "100vh",
        gap: "0.8rem",
      }}
    ></div>
  );
};

export default BasicsOfMotion;
